const Button=({children, variant="primary"})=>{

    const getRespectiveButton=(variant)=>{
        switch
    }


    return(
        <>
        <button>{getRespectiveButton(variant)}</button>
        </>
    )
}
export default Button